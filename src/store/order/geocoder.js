/*import { createApi, fetchBaseQuery } from "@reduxjs/toolkit/dist/query";

const geocoderApi = createApi({
  reducerPath: "geocoder",
  baseQuery: fetchBaseQuery({
    baseUrl: "https://geocode-maps.yandex.ru/1.x/",
  }),
  endpoints: (builders) => ({
    geocode: builders.query({
      query: (adress) => ({
        url: "",
      }),
    }),
  }),
});*/
